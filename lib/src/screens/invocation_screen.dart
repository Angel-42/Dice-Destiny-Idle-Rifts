import 'package:flutter/material.dart';

class InvocationScreen extends StatelessWidget {
  const InvocationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Invocation Screen',
        ),
      ),
    );
  }
}
