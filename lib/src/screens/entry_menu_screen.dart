import 'package:flutter/material.dart';

class EntryMenuScreen extends StatelessWidget {
  const EntryMenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
